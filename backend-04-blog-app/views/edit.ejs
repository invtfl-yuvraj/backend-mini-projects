<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Post</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="/stylesheets/style.css">
</head>

<body>

    <div class="main w-full min-h-screen bg-zinc-900 px-16 pt-20 flex flex-col gap-10 items-center">
        <h1 class="text-3xl text-blue-400 font-bold">Edit Post</h1>
        <div class="w-full h-[1px] bg-zinc-100 rounded-md"></div>


        <div class="posts flex flex-col justify-start items-center flex-wrap gap-4 min-h-full w-full pl-4 col-span-2">

            <!-- single post  -->

            <div class="w-full">

                <div class="user h-full w-full flex justify-center ">

                    <form class="flex flex-col justify-center gap-4" action="/update/<%=post._id%>" method = "post">
                        <input class="bg-zinc-800 rounded-md p-4 min-w-[600px] text-white" type="text" name="title" id=""
                            placeholder="Post Title" value="<%=post.title%>">
                        <textarea name="content" placeholder="What's on your mind ?"
                            class="bg-zinc-800 rounded-md p-4 min-w-[600px] min-h-[200px] text-white"><%=post.content%></textarea>

                        <div class="flex justify-between w-full">

                            <!-- Go back  -->
                            <a class="block-inline" href="/profile/<%=post.user._id%>">
                                <p class="bg-blue-400 rounded-lg p-2 px-12 text-black">
                                    Go back
                                </p>
                            </a>

                            <!-- Submit button -->

                            <input type="submit" value="Update" class="bg-yellow-400 rounded-lg p-2 px-12 text-black cursor-pointer">
                        </div>
                    </form>

                </div>
            </div>
        </div>
    </div>
</body>

</html>